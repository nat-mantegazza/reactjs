import React from 'react'

const ItemList = ({productos}) => {
  return (
    <div>
        {
            productos.map((p)) => {
                return (
                    <div>

                    </div>
                )
            }
        }
    
    
    </div>
  )
}

export default ItemList